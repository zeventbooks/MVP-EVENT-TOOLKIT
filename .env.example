# Environment Variables Template
# Copy to .env.local and fill in your values
#
# Single source of truth: config/environments.js
# See DEPLOYMENT.md for full environment documentation

# ============================================
# BASE_URL - Single Toggle for Test Targets
# ============================================
# Tests default to STAGING (https://stg.eventangle.com) for safety.
# Override with BASE_URL to target other environments.
#
# IMPORTANT: Tests use BASE_URL without path suffix - they append their own paths
# like /status, /{brand}/status, /manage, etc.

# Admin key for authentication
ADMIN_KEY=your_admin_key_here

# Brand ID (root, abc, cbc, cbl)
BRAND_ID=root

# ============================================
# Environment Configuration
# ============================================
#
# STAGING (DEFAULT - no BASE_URL needed):
#   npm run test:smoke
#
# PRODUCTION (explicit only):
#   npm run test:prod:smoke
#   USE_PRODUCTION=true npm run test:smoke
#   BASE_URL="https://www.eventangle.com" npm run test:smoke
#
# GAS Direct (debugging):
#   BASE_URL="https://script.google.com/macros/s/{ID}/exec" npm run test:smoke
#
# Local Development:
#   BASE_URL="http://localhost:3000" npm run test:smoke

# ============================================
# Environment URL Examples
# ============================================

# STAGING - Safe sandbox (DEFAULT - uncomment to override default)
# BASE_URL=https://stg.eventangle.com

# Production (Cloudflare Workers)
# BASE_URL=https://www.eventangle.com

# GAS Direct (debugging only)
# BASE_URL=https://script.google.com/macros/s/AKfycbyS1cW9VhviR-Jr8AmYY_BAGrb1gzuKkrgEBP2M3bMdqAv4ktqHOZInWV8ogkpz5i8SYQ/exec

# QA Environment
# BASE_URL=https://zeventbooks.com

# Local Development
# BASE_URL=http://localhost:3000

# ============================================
# Usage
# ============================================
#
# 1. cp .env.example .env.local
# 2. Edit .env.local with your ADMIN_KEY
# 3. source .env.local
# 4. npm run test:smoke  # Uses STAGING by default
#
# Or run directly:
#   npm run test:smoke        # Uses STAGING (safe)
#   npm run test:prod:smoke   # Uses PRODUCTION (explicit)
