name: Stage 1 - Build & Deploy

on:
  push:
    branches: [ main, 'claude/**' ]
  pull_request:
    branches: [ main ]

jobs:
  # ============================================================================
  # STAGE 1: BUILD, TEST & DEPLOY
  # ============================================================================

  unit-tests:
    name: ✅ Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -- --coverage

      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info

  contract-tests:
    name: 📋 Contract Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run contract tests
        run: npm run test:contract

      - name: Run Triangle contract tests
        run: |
          npm run test:triangle:before:contract
          npm run test:triangle:during:contract
          npm run test:triangle:after:contract
          npm run test:triangle:all:contract

  deploy-to-apps-script:
    name: 🚀 Deploy to Apps Script API
    runs-on: ubuntu-latest
    needs: [unit-tests, contract-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    outputs:
      deployment_url: ${{ steps.deploy.outputs.url }}
      root_url: ${{ steps.urls.outputs.root_url }}
      abc_url: ${{ steps.urls.outputs.abc_url }}
      cbc_url: ${{ steps.urls.outputs.cbc_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Deploy to Apps Script via clasp
        id: deploy
        env:
          CLASPRC_JSON: ${{ secrets.OAUTH_CREDENTIALS }}
          DEPLOYMENT_ID: ${{ secrets.DEPLOYMENT_ID }}
        run: |
          echo "🔐 Setting up clasp credentials..."
          echo "$CLASPRC_JSON" > ~/.clasprc.json

          echo "🚀 Deploying to Apps Script..."
          npx clasp push --force

          echo "📦 Updating deployment..."
          # Check if we have a deployment ID to update
          if [ -n "$DEPLOYMENT_ID" ]; then
            echo "Updating existing deployment: $DEPLOYMENT_ID"
            DEPLOY_OUTPUT=$(npx clasp deploy -i "$DEPLOYMENT_ID" -d "Stage 1 Deploy $(date -Iseconds)" 2>&1)
          else
            echo "Creating new deployment (no DEPLOYMENT_ID secret set)"
            DEPLOY_OUTPUT=$(npx clasp deploy -d "Stage 1 Deploy $(date -Iseconds)" 2>&1)
          fi

          echo "$DEPLOY_OUTPUT"
          VERSION_ID=$(echo "$DEPLOY_OUTPUT" | grep -oP 'Created version \K\d+' || echo "$DEPLOY_OUTPUT" | grep -oP 'Updated deployment \K\w+' || echo "unknown")

          # Get the web app URL from clasp deployments
          echo "📋 Fetching deployment URL..."
          DEPLOYMENTS_OUTPUT=$(npx clasp deployments 2>&1)
          echo "$DEPLOYMENTS_OUTPUT"

          # Extract the web app URL (look for @HEAD or latest deployment)
          WEB_APP_URL=$(echo "$DEPLOYMENTS_OUTPUT" | grep -oP 'https://script\.google\.com/macros/s/[^/]+/exec' | head -1)

          if [ -z "$WEB_APP_URL" ]; then
            echo "⚠️ Warning: Could not extract web app URL from deployments"
            echo "Please check clasp deployments manually"
            exit 1
          fi

          echo "url=$WEB_APP_URL" >> $GITHUB_OUTPUT
          echo "✅ Deployment complete! Base URL: $WEB_APP_URL"

      - name: Generate tenant URLs
        id: urls
        run: |
          BASE_URL="${{ steps.deploy.outputs.url }}"

          # Generate tenant-specific URLs
          ROOT_URL="${BASE_URL}?p=events&tenant=root"
          ABC_URL="${BASE_URL}?p=events&tenant=abc"
          CBC_URL="${BASE_URL}?p=events&tenant=cbc"

          echo "root_url=$ROOT_URL" >> $GITHUB_OUTPUT
          echo "abc_url=$ABC_URL" >> $GITHUB_OUTPUT
          echo "cbc_url=$CBC_URL" >> $GITHUB_OUTPUT

          echo "📋 Tenant URLs generated:"
          echo "   🎯 ROOT: $ROOT_URL"
          echo "   🎯 ABC:  $ABC_URL"
          echo "   🎯 CBC:  $CBC_URL"

      - name: Create deployment summary
        run: |
          echo "## 🚀 Stage 1 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Tests Passed" >> $GITHUB_STEP_SUMMARY
          echo "- Unit Tests" >> $GITHUB_STEP_SUMMARY
          echo "- Contract Tests" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🌐 Deployment URLs" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Base URL:** ${{ steps.deploy.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tenant URLs for Manual Verification:**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- 🎯 **ROOT:** [${{ steps.urls.outputs.root_url }}](${{ steps.urls.outputs.root_url }})" >> $GITHUB_STEP_SUMMARY
          echo "- 🎯 **ABC:** [${{ steps.urls.outputs.abc_url }}](${{ steps.urls.outputs.abc_url }})" >> $GITHUB_STEP_SUMMARY
          echo "- 🎯 **CBC:** [${{ steps.urls.outputs.cbc_url }}](${{ steps.urls.outputs.cbc_url }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "1. **Manually verify** the deployment URLs above" >> $GITHUB_STEP_SUMMARY
          echo "2. Once verified, trigger **Stage 2 Tests** manually:" >> $GITHUB_STEP_SUMMARY
          echo "   - Go to **Actions** → **Stage 2 - Testing & QA**" >> $GITHUB_STEP_SUMMARY
          echo "   - Click **Run workflow**" >> $GITHUB_STEP_SUMMARY
          echo "   - Paste the Base URL: \`${{ steps.deploy.outputs.url }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
